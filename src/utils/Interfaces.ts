export interface movie{
    movie: string,
    year: number,
    release_date: string,
    director: string,
    character: string,
    movie_duration: string,
    timestamp: string,
    full_line: string,
    current_wow_in_movie: number,
    total_wows_in_movie: number,
    poster: string,
}
